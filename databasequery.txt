select photoid,postingDate,photoposter FROM photo where ("TestUser" in (select username_follower from follow where username_followed = photoposter AND followStatus = 1) AND allFollowers=1) OR photoid in (Select photoid from sharedwith as s1 where (memberusername = photoposter AND "TestUser" IN (select member_username from belongto as b2 where owner_username=s1.ownerUsername AND b2.groupName = s1.groupName)))